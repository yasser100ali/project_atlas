fastapi
python-dotenv
# Pin to a recent OpenAI SDK that includes `openai.types.responses`
openai>=1.45.0
pydantic
python-multipart
requests
PyYAML
# Agents SDK (provides `agents` package). Latest available on PyPI is 0.2.6.
openai-agents==0.2.6
pypdf

rendercv[full]